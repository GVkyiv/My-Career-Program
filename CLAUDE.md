@AGENTS.md

## Claude Code

Источник правды для всех агентов — `AGENTS.md`. Этот файл лишь мост:
Claude Code читает `CLAUDE.md` и не читает `AGENTS.md` без импорта.
Правила дописывать в `AGENTS.md`, а не сюда.
